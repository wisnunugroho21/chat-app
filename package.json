{
  "name": "chat-app",
  "type": "module",
  "private": true,
  "//dev": "--no-fork is required: the forked dev server proxies HTTP but not WebSocket upgrades, so /_ws 404s without it.",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --no-fork",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "firebase": "^12.17.1",
    "firebase-admin": "^14.2.0",
    "mongodb": "^7.5.0",
    "nuxt": "^4.5.2",
    "vue": "^3.5.41",
    "vue-router": "^5.2.0"
  }
}

# ---------------------------------------------------------------------------
# Sessions
#
# Seals the signed-in cookie. Must be at least 32 characters. Leave it unset
# and the server invents one per process, which works but signs everybody out
# on every restart.
#
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ---------------------------------------------------------------------------

NUXT_SESSION_PASSWORD=

# ---------------------------------------------------------------------------
# MongoDB
#
# Optional. Without it the app keeps everything in memory and starts from the
# seed data on every restart. With it, chats, messages, delivery receipts and
# push tokens survive.
#
#   local:  mongodb://127.0.0.1:27017
#   Atlas:  mongodb+srv://<user>:<password>@<cluster>.mongodb.net
# ---------------------------------------------------------------------------

NUXT_MONGODB_URI=
NUXT_MONGODB_DB=chat-app

# ---------------------------------------------------------------------------
# WebRTC calling
#
# Optional. The defaults use public STUN, which covers most networks; two tabs
# on one machine need nothing at all. Calls between networks that sit behind
# symmetric NAT only connect through a TURN relay — set one here if you have
# one (coturn, or a hosted service).
# ---------------------------------------------------------------------------

NUXT_PUBLIC_WEBRTC_STUN_URLS=stun:stun.l.google.com:19302,stun:stun1.l.google.com:19302
NUXT_PUBLIC_WEBRTC_TURN_URL=
NUXT_PUBLIC_WEBRTC_TURN_USERNAME=
NUXT_PUBLIC_WEBRTC_TURN_CREDENTIAL=

# ---------------------------------------------------------------------------
# Firebase Cloud Messaging
#
# Everything here is optional. With no values set, messages still send and
# receive over the WebSocket while a tab is open — only background push is off.
# ---------------------------------------------------------------------------

# Web app config: Firebase console → Project settings → Your apps → Web app
NUXT_PUBLIC_FIREBASE_API_KEY=
NUXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NUXT_PUBLIC_FIREBASE_PROJECT_ID=
NUXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NUXT_PUBLIC_FIREBASE_APP_ID=

# Project settings → Cloud Messaging → Web Push certificates → key pair
NUXT_PUBLIC_FIREBASE_VAPID_KEY=

# Service account, for sending: Project settings → Service accounts →
# Generate new private key. Keep the private key on one line with literal \n.
NUXT_FCM_PROJECT_ID=
NUXT_FCM_CLIENT_EMAIL=
NUXT_FCM_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
